# 更新日志

## v2.1.7

New Features:
- 添加一个已显示注册的点歌命令，方便其他帮助插件生成菜单时识别。
- 允许 AI 播放歌曲请求时选择特定的音乐平台。

Enhancements:
- 将已下载的歌曲文件存储在 Astrbot 的临时目录中自动管理缓存，并按需创建下载目录。

## v2.1.6

新功能：
- 新增 `cz_card` 发送模式，通过可复用的 HTTP 会话从外部 API 获取已签名的 CZ 音乐卡片来发送 CZ 音乐卡片。

改进：
- 更新发送端的歌曲选择文案，展示歌曲名称，在缺少艺人信息时回退显示作者。
- 简化文本发送模式，仅输出歌曲音频 URL，并调整日志消息。

## v2.1.5

新特性：
- 支持通过 QQ 官方机器人交互按钮进行点歌选择，除文本和图片模式外新增按钮模式。
- 按用户/会话追踪点歌消息的 ID，以便在歌曲被选中后进行撤回。

缺陷修复：
- 在有相关配置时，确保点歌消息会被撤回，而不是依赖单次调用的本地状态。

改进：
- 将点歌消息的撤回逻辑集中到发送端内部，并包含 QQ 官方机器人平台和 aiocqhttp 的平台特定删除实现。
- 简化与配置相关的日志输出，并将部分警告信息改为更清晰的英文措辞。
- 从核心和插件生命周期方法中移除冗余的文档字符串，使代码更简洁。

## v2.1.4

新特性：
- 为记录和文件分别引入通过链接与本地文件的独立发送模式，从而更精细地控制歌曲的发送方式。

改进：
- 优化平台能力检查逻辑，对记录和文件发送改为使用“不支持模式列表”而非“支持模式列表”。
- 改善在记录和文件发送中音频 URL 缺失或下载失败时的错误处理与日志记录。
- 更新用户输入解析与模式别名，以支持扩展后的发送模式集合，并提供更清晰的校验提示信息。
- 更新 README 文档，描述新的发送模式选项及其数字映射关系。

## v2.1.3

新功能：

- 添加查歌词 LLM Tool，使模型可在用户表达歌词查询需求时自动搜索并发送歌词图片。

增强改进：

- 将歌词渲染器的默认样式配置整理为独立主题类，使其结构与歌曲卡片渲染器保持一致。
- 将 VideoCardRenderer 重命名为 CardRenderer，并在整个代码库中将其参数从仅适用于视频泛化为适用于媒体。

Bug 修复：

- 修复单句歌词过长时歌词图片左右内容显示不全的问题，歌词渲染宽度会根据最长单句自适应扩展。

## v2.1.2

新功能：

- 为 TXQQ 音乐支持自定义音乐卡片负载，提供明确的音频、封面、标题和艺人字段。
- 添加本地图片式歌曲选择模式，使用渲染后的歌曲列表卡片。

增强改进：

- 将歌曲选择消息的删除延后到用户选择歌曲之后，而不是依赖基于超时的召回机制。
- 放宽卡片模式的平台限制，使卡片消息在所有 aiocqhttp 播放器上可用。
- 为 Song 模型增加可选的 source 字段，用于追踪来源平台。
- 明确并扩展 TXQQ 音乐平台的搜索关键词，从而减少误触点歌。
- 移除基本没人使用的歌单功能，删除相关命令与本地数据库逻辑以精简代码。

## v2.1.1

增强改进：

- 优化歌词发送流程：当已存储的歌词字段是一个 URL 时，可透明地解析远程歌词内容并进行发送。

## v2.1.0

通过插件配置而非硬编码的方式，使媒体发送模式的平台支持变为可配置。

新功能：

- 允许通过插件设置来配置哪些平台支持录制和文件发送模式。

增强改进：

- 重构发送模式支持检查，从使用硬编码的平台集合改为使用由配置驱动的平台列表。

### v2.0.9

更新音乐插件的配置路径并扩展发送者模式的平台支持，同时为基于 URL 的音频发送增加安全检查。

Bug 修复：

- 修正插件字体和数据目录的解析逻辑，改为使用插件专用路径。
- 当歌曲没有音频 URL 时，避免尝试执行基于 URL 的音频发送。

功能增强：

- 将录音和文件发送模式的支持矩阵扩展到更多消息平台。

### v2.0.8

支持自定义歌曲发送格式，包括卡片、语音、文件、文本等多种格式，满足不同场景下的发送需求。

**使用方式**:

当唤出歌曲列表后,输入<列表序号> <模式> 即可选择指定模式发送歌曲

例如：

`1 卡片`  -> 发送列表第一首歌曲为卡片格式

`2 语音`  -> 发送列表第二首歌曲为语音格式

`3 文件`  -> 发送列表第三首歌曲为文件格式

`4 文本`  -> 发送列表第四首歌曲为文本格式

`1 1` -> 发送列表第一首歌曲为卡片格式

`2 2` -> 发送列表第二首歌曲为语音格式

`3 3` -> 发送列表第三首歌曲为文件格式

`4 4` -> 发送列表第四首歌曲为文本格式

`1 card` -> 发送列表第一首歌曲为卡片格式

`2 record` -> 发送列表第二首歌曲为语音格式

`3 file` -> 发送列表第三首歌曲为文件格式

`4 text` -> 发送列表第四首歌曲为文本格式
